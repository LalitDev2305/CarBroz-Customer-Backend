/** Coordinates is an exported foundation/kernel contract/implementation; see the owning README for lifecycle and extension rules. */
export interface Coordinates {
  latitude: number;
  longitude: number;
}

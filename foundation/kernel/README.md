# Foundation Kernel (`foundation/kernel/`)

Owns the minimal domain-independent kernel contracts and primitives shared across bounded contexts. It must remain free of product-specific business ownership and must not become a generic dumping ground.

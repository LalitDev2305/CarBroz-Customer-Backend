# SDUI Registry (`sdui/registry/`)

Owns SDUI definition registration, lifecycle, lookup, versioning, and publication policy while consuming the generic UI SDK contract. It does not redefine UI schema vocabulary or product-domain business policy.

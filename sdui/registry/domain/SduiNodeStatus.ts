/** SduiNodeStatus is an exported sdui/registry contract/implementation; see the owning README for lifecycle and extension rules. */
export type SduiNodeStatus = 'ACTIVE' | 'INACTIVE' | 'DEPRECATED';

# SDUI UI SDK (`sdui/ui-sdk/`)

Owns the generic, product-neutral SDUI schema, validation, and rendering-contract vocabulary exposed through `@carbroz/ui-sdk`. Registry lifecycle and business-specific screen composition are separate authorities.

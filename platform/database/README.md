# Database Platform (`platform/database/`)

Owns shared technical database and Prisma infrastructure, including transaction and persistence primitives. Domain repository contracts and business persistence policy remain with their owning bounded contexts.

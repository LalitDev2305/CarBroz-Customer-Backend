/** NotificationConfig is an exported platform/integrations contract/implementation; see the owning README for lifecycle and extension rules. */
export interface NotificationConfig {
  fcmApiKey?: string;
  msg91AuthKey?: string;
  resendApiKey?: string;
}

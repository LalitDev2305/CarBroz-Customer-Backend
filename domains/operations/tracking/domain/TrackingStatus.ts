/** TrackingStatus is an exported domains/operations contract/implementation; see the owning README for lifecycle and extension rules. */
export type TrackingStatus = 'ACTIVE' | 'PAUSED' | 'COMPLETED' | 'CANCELLED';

/** RolePermission is an exported domains/identity contract/implementation; see the owning README for lifecycle and extension rules. */
export interface RolePermission {
  roleId: number;
  permissionId: number;
}

/** NotificationStatus is an exported domains/communications contract/implementation; see the owning README for lifecycle and extension rules. */
export type NotificationStatus = 'SENT' | 'DELIVERED' | 'READ' | 'FAILED';

/** NotificationChannel is an exported domains/communications contract/implementation; see the owning README for lifecycle and extension rules. */
export type NotificationChannel = 'PUSH' | 'SMS' | 'EMAIL';

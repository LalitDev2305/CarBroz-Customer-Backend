/** ReviewStatus is an exported domains/engagement contract/implementation; see the owning README for lifecycle and extension rules. */
export type ReviewStatus = 'PUBLISHED' | 'FLAGGED' | 'REJECTED';

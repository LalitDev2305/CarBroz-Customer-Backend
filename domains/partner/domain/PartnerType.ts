/** PartnerType is an exported domains/partner contract/implementation; see the owning README for lifecycle and extension rules. */
export enum PartnerType {
  INDIVIDUAL = 'INDIVIDUAL',
  ORGANIZATION = 'ORGANIZATION'
}

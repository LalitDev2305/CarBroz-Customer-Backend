/** PartnerMemberRole is an exported domains/partner contract/implementation; see the owning README for lifecycle and extension rules. */
export enum PartnerMemberRole {
  OWNER = 'OWNER',
  MANAGER = 'MANAGER',
  SUPERVISOR = 'SUPERVISOR',
  EMPLOYEE = 'EMPLOYEE'
}

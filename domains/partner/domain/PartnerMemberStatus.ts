/** PartnerMemberStatus is an exported domains/partner contract/implementation; see the owning README for lifecycle and extension rules. */
export enum PartnerMemberStatus {
  ACTIVE = 'ACTIVE',
  INVITED = 'INVITED',
  SUSPENDED = 'SUSPENDED'
}

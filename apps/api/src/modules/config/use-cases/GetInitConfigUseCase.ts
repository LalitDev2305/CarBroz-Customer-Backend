// Temporary compatibility surface for the pre-closeout API tree.
// Configuration is the single application owner; the closeout transform removes this API-local path.
export { GetInitConfigUseCase } from '@carbroz/domain-configuration';

/**
 * Transitional compatibility export.
 * Canonical ownership is apps/api/src/bootstrap/lifecycle/toExecutionContext.ts.
 * Remove this shim after all checked-in API consumers are migrated during CW2.
 */
export { toExecutionContext } from '../bootstrap/lifecycle/toExecutionContext.js';

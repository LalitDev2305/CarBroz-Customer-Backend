# Architecture Tests

These tests enforce non-negotiable rules from `docs/MASTER-BACKEND-CONSTITUTION.md`.

They are intentionally repository-aware and may inspect the Git index. A passing compile is not sufficient when tracked generated artifacts, duplicate architecture authorities, or forbidden dependencies remain.
